// Eolax brand configuration. Swap these for the real brand assets when
// available — the colors live in src/app/globals.css (:root brand variables).
export const BRAND = {
  // App name shown across headers and the login screen.
  name: "Eolax Avatar Studio",
  shortName: "Eolax",
  tagline: "Estudio de avatares con IA",
  // To use a real logo image instead of the text wordmark, set logoSrc to a
  // path under /public (e.g. "/eolax-logo.svg") and render it in BrandMark.
  logoSrc: null as string | null,
};
